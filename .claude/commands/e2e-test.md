---
description: Run the op-e2e full-stack test suite
argument-hint: [suite or test name]
---

Run the `e2e-testing` skill for `$ARGUMENTS` (or the full suite if no
argument given). Report pass/fail per category (round-trip, failure-mode,
batch/DA) and investigate any flaky test rather than re-running until
green.
