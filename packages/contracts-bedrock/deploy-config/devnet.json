{
  "_comment": "kovanica-chain LOCAL DEVNET intent. For the Kurtosis optimism-package these values are applied via op_contract_deployer_params.global_deploy_overrides in ../../devnet/network_params.yaml; keep the two in sync. Devnet timers are deliberately shortened and are NOT safe for any public network.",

  "l1ChainID": 3151908,
  "l2ChainID": 2900,
  "l2BlockTime": 2,

  "useFaultProofs": true,
  "respectedGameType": 0,

  "faultGameAbsolutePrestate": "0x03c7ae758795765c6664a5d39bf63841c71ff191e9189522bad8ebff5d4eca98",
  "_faultGameAbsolutePrestate_note": "PLACEHOLDER — regenerate the Cannon64 prestate for kovanica's genesis/rollup (Stage 1b) and replace. Must equal the hash of the prestate file the challenger serves.",

  "faultGameMaxDepth": 73,
  "faultGameSplitDepth": 30,
  "faultGameClockExtension": 0,
  "faultGameMaxClockDuration": 300,
  "faultGameWithdrawalDelay": 120,

  "proofMaturityDelaySeconds": 120,
  "disputeGameFinalityDelaySeconds": 600,
  "_timers_note": "Devnet fast-iteration values (kept in lockstep with network_params.yaml, enforced by scripts/check-config-consistency.py) so a full deposit->withdraw->prove->finalize round trip finishes in minutes. NOT for any public network.",

  "preimageOracleMinProposalSize": 126000,
  "preimageOracleChallengePeriod": 300,

  "fundDevAccounts": true,
  "_ownership_note": "Devnet only: dev accounts funded and no SAFE. Stage 2+ (Sepolia/mainnet) MUST set fundDevAccounts:false and use SAFE-based proxyAdminOwner/guardian, per CLAUDE.md §3 and docs/bootstrapping-fault-proof-op-stack-2026.md."
}
